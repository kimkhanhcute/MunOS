#pragma once

bool strcmp(const char* a, const char* b);

int strlen(const char* str);

bool starts_with(
    const char* str,
    const char* prefix
);
void int_to_string(
    int value,
    char* buffer
);
